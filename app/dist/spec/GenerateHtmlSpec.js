// Generated by CoffeeScript 1.9.3
(function() {
  describe("GenerateHtml", function() {
    var element, generateHtml;
    element = void 0;
    generateHtml = void 0;
    it("should be able to generate the html of an image", function() {
      var htmlExpect;
      element = new Element('image', '', 0, 0);
      generateHtml = new GenerateHtml();
      htmlExpect = '<img src="" style="position:absolute; top:0px; left:0px;"/>';
      return expect(generateHtml.getHtml(element, 0, 0)).toEqual(htmlExpect);
    });
    it("should be able to generate the html of an image 100*100", function() {
      var htmlExpect;
      element = new Element('image', '', 100, 100);
      generateHtml = new GenerateHtml();
      htmlExpect = '<img src="" style="position:absolute; top:0px; left:0px;"/>';
      return expect(generateHtml.getHtml(element, 0, 0)).toEqual(htmlExpect);
    });
    it("should be able to generate the html of an image 100*100 in random position", function() {
      var htmlExpect;
      element = new Element('image', '', 100, 100);
      generateHtml = new GenerateHtml();
      htmlExpect = '<img src="" style="position:absolute; top:1200px; left:400px;"/>';
      return expect(generateHtml.getHtml(element, 12, 4)).toEqual(htmlExpect);
    });
    return it("should be able to generate the html of a video", function() {
      var htmlExpect;
      element = new Element('video', 'link', 0, 0);
      generateHtml = new GenerateHtml();
      htmlExpect = '<video id="video" autoplay src="link"></video>';
      return expect(generateHtml.getHtml(element, 0, 0)).toEqual(htmlExpect);
    });
  });

}).call(this);
