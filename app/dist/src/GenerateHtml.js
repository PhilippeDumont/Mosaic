// Generated by CoffeeScript 1.9.3
(function() {
  var GenerateHtml;

  GenerateHtml = (function() {
    function GenerateHtml() {}

    GenerateHtml.prototype.getHtml = function(element, x, y) {
      var html;
      html = '';
      if (element.type === 'image') {
        return this._getHtmlOfImage(element, x, y);
      } else if (element.type === 'video') {
        return this._getHtmlOfVideo(element, x, y);
      }
    };

    GenerateHtml.prototype._getHtmlOfImage = function(element, x, y) {
      var html;
      html = '<img src="';
      html += element.link;
      html += '" style="position:absolute; top:';
      html += x * element.height;
      html += 'px; left:';
      html += y * element.width;
      html += 'px;"/>';
      return html;
    };

    GenerateHtml.prototype._getHtmlOfVideo = function(element, x, y) {
      var html;
      html = '<video id="video" autoplay src="';
      html += element.link;
      html += '"></video>';
      return html;
    };

    return GenerateHtml;

  })();

  window.GenerateHtml = GenerateHtml;

}).call(this);
